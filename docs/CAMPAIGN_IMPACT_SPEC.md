# Campaign & Media Impact Specification

> **Version**: 1.0.0  
> **Last Updated**: 2026-01-23  
> **Status**: Implementation Ready  
> **Position**: Cause Analysis (원인 분석)

---

## 1. Campaign & Media Impact의 정체성

### 1.1 정의

Campaign & Media Impact는 Review Flow에서 관측된 **'검토 상태 변화'**가  
**어떤 캠페인·광고·이벤트에 의해 발생했는지**를 해석하는 원인 분석 화면이다.

```
이 메뉴는
  - 캠페인 성과를 자랑하는 곳 ❌
  - 광고 KPI를 평가하는 곳 ❌
  - "왜 이런 흐름이 만들어졌는가"를 설명하는 곳 ⭕
```

### 1.2 답해야 할 4가지 질문

| # | 질문 | 해석 |
|---|------|------|
| 1 | 어떤 캠페인/이벤트가 Review Flow를 실제로 이동시켰는가 | 트리거 식별 |
| 2 | 그 이동은 일시적 반응인가, 지속 검토로 이어졌는가 | 지속성 판단 |
| 3 | 광고는 증폭자(Amplifier)였는가, 왜곡자(Distorter)였는가 | 역할 분류 |
| 4 | 어떤 미디어 조합이 의미 있는 검토 전환을 만들었는가 | 조합 효과 |

### 1.3 의도적으로 배제하는 것

❌ 매체별 성과 랭킹  
❌ CPC / CTR 비교  
❌ 광고비 효율 분석  
❌ 단기 ROI 계산

---

## 2. 페이지 레이아웃

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Campaign & Media Impact                          │
│                  캠페인·미디어 영향 분석 (Cause Analysis)            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ [A] Impact Summary                                           │    │
│  │     "최근 캠페인들이 의미 있었는가"                           │    │
│  │     캠페인별 영향 분류 + 한 줄 해석                           │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ [B] Campaign-to-Flow Mapping                                 │    │
│  │     "이 캠페인이 Review Flow의 어디를 움직였는가"             │    │
│  │     X축: 시간 (전/중/후), Y축: 단계 (Initial/Deep)           │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌───────────────────────────┐  ┌───────────────────────────────┐   │
│  │ [C] Before/After          │  │ [D] Media Role Interpretation │   │
│  │     Review Shift          │  │     미디어 역할 분류           │   │
│  │     캠페인 전후 비교       │  │                               │   │
│  └───────────────────────────┘  └───────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘

위 → 아래: 요약 → 원인 → 증거 → 해석
모든 컴포넌트는 Review Flow의 변화와 직접 연결
```

---

## 3. [A] Impact Summary

### 3.1 목적
"최근 캠페인들이 의미 있었는가"를 한눈에 판단

### 3.2 구성 요소

| 요소 | 설명 |
|------|------|
| 캠페인명 | CES, Tech Event, 집중 광고 캠페인 등 |
| 기간 | 캠페인 실행 기간 |
| 영향 분류 (Label) | Amplifier / Distorter / Neutral |
| 한 줄 해석 | Review Flow 변화 결과 요약 |

### 3.3 영향 분류 정의

| 분류 | 의미 | 판단 기준 |
|------|------|----------|
| **Amplifier** | 기존 검토를 강화 | 캠페인 후 Deep Review 비중 증가 + 유지 |
| **Distorter** | 단기 반응만 유발 | 캠페인 중 상승, 종료 후 급락 |
| **Neutral** | 영향 미미 | 유의미한 변화 없음 |

### 3.4 한 줄 해석 예시

```
- "Initial → Deep 이동 발생, 종료 후에도 유지"
- "광고 종료 후 검토 유지 실패"
- "기존 검토 흐름에 추가 영향 미미"
```

### 3.5 포함하지 않는 것

```
❌ 노출 수
❌ 클릭 수
❌ CTR
❌ 광고비
```

---

## 4. [B] Campaign-to-Flow Mapping

### 4.1 목적
"이 캠페인이 Review Flow의 어디를 움직였는가"

### 4.2 축 구성

| 축 | 의미 | 값 |
|----|------|-----|
| X축 | 시간 | 캠페인 전 / 중 / 후 |
| Y축 | Review Flow 단계 | Initial Review / Deep Review |

### 4.3 출력 요소

```typescript
interface CampaignFlowMapping {
  campaignId: string;
  campaignName: string;
  period: {
    before: { start: string; end: string };
    during: { start: string; end: string };
    after: { start: string; end: string };
  };
  technologies: {
    id: string;
    name: string;
    stageBefore: 'initial' | 'deep';
    stageDuring: 'initial' | 'deep';
    stageAfter: 'initial' | 'deep';
    movement: 'advanced' | 'maintained' | 'declined';
  }[];
}
```

### 4.4 시각화 형태

```
                Campaign Period
                    ┌────┐
    Deep    ────────│ ◉──│──◉────◉────  (Digital Cockpit: 유지)
                    │    │
    Initial ──◉─────│──◉─│────────────  (IVI: 상승→유지실패)
                    │    │
            ────────┴────┴────────────→
              Before   During   After
```

### 4.5 광고 반영 방식

- Paid Media는 **이동을 촉발한 트리거**로만 표시
- 광고 집행 종료 이후 **이동 유지 여부** 강조

---

## 5. [C] Before / After Review Shift

### 5.1 목적
"이동이 실제로 있었는지 증거를 보여준다"

### 5.2 구성 요소

```typescript
interface ReviewShift {
  period: {
    before: string;  // "2025-12-01 ~ 2025-12-31"
    after: string;   // "2026-01-01 ~ 2026-01-23"
  };
  stageComparison: {
    initial: { before: number; after: number; change: number };
    deep: { before: number; after: number; change: number };
  };
  technologyMovements: {
    id: string;
    name: string;
    movement: 'advanced' | 'maintained' | 'declined';
    from: 'initial' | 'deep';
    to: 'initial' | 'deep';
  }[];
}
```

### 5.3 시각화

```
┌─────────────────────────────────────────────────────┐
│  Stage Distribution                                  │
│                                                      │
│  Initial Review                                      │
│  Before ████████████████ 65%                        │
│  After  ████████████ 52%          ▼ -13%            │
│                                                      │
│  Deep Review                                         │
│  Before ██████████ 35%                              │
│  After  ████████████████ 48%      ▲ +13%            │
└─────────────────────────────────────────────────────┘
```

### 5.4 해석 기준

| 기준 | 판단 |
|------|------|
| 단기 스파이크 | ❌ 의미 없음 |
| 캠페인 이후 일정 기간 유지 | ⭕ 의미 있음 |

---

## 6. [D] Media Role Interpretation

### 6.1 목적
"어떤 미디어가 어떤 역할을 했는가"

### 6.2 미디어 역할 분류

| 역할 | 의미 | 특징 |
|------|------|------|
| **Igniter** | 검토 시작 유발 | 첫 접촉 발생 비중 높음 |
| **Accelerator** | 초기→심화 이동 가속 | 재방문, 다중 콘텐츠 소비 유발 |
| **Supporter** | 검토 유지 기여 | 리마인더 역할, 이탈 방지 |
| **Noise** | 일시적 반응만 유발 | 높은 이탈률, 낮은 전환 |

### 6.3 데이터 구조

```typescript
interface MediaRole {
  campaignId: string;
  media: {
    channel: 'google_ads' | 'linkedin_ads' | 'youtube_ads' | 'social' | 'video';
    role: 'igniter' | 'accelerator' | 'supporter' | 'noise';
    technologies: string[];  // 영향받은 기술
    evidence: string;        // 역할 판단 근거
  }[];
}
```

### 6.4 시각화 예시

```
┌──────────────────────────────────────────────────────────┐
│  Media Role Analysis                                      │
│                                                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │ Google Ads  │  │ LinkedIn    │  │ YouTube     │       │
│  │             │  │             │  │             │       │
│  │ [Igniter]   │  │ [Accelerator]│ │ [Supporter] │       │
│  │             │  │             │  │             │       │
│  │ IVI, ADAS   │  │ Digital     │  │ Vehicle     │       │
│  │             │  │ Cockpit     │  │ Vision      │       │
│  └─────────────┘  └─────────────┘  └─────────────┘       │
│                                                           │
│  해석: LinkedIn이 심화 검토 전환의 핵심 가속 역할 수행     │
└──────────────────────────────────────────────────────────┘
```

---

## 7. 네비게이션 연결

### 7.1 Campaign & Media Impact → 다음 메뉴

| 액션 | 이동 대상 | 목적 |
|------|----------|------|
| 특정 캠페인 클릭 | Review Flow (기간 필터 적용) | 해당 기간 흐름 확인 |
| 미디어 역할 클릭 | Detail / Reference | 지표 상세 확인 |

```
👉 항상 Review Flow를 기준으로 왕복 이동
```

---

## 8. 데이터 구조

### 8.1 Campaign Data

```typescript
interface Campaign {
  id: string;
  name: string;
  type: 'event' | 'advertising' | 'content';
  period: {
    start: string;
    end: string;
  };
  impact: 'amplifier' | 'distorter' | 'neutral';
  summary: string;  // 한 줄 해석
  metrics: {
    initialBefore: number;
    initialAfter: number;
    deepBefore: number;
    deepAfter: number;
    retention: number;  // 종료 후 유지율
  };
}
```

### 8.2 Demo Data

```typescript
const DEMO_CAMPAIGNS: Campaign[] = [
  {
    id: 'ces-2026',
    name: 'CES 2026',
    type: 'event',
    period: { start: '2026-01-07', end: '2026-01-10' },
    impact: 'amplifier',
    summary: 'Initial → Deep 이동 발생, 종료 후에도 +18% 유지',
    metrics: {
      initialBefore: 65,
      initialAfter: 52,
      deepBefore: 35,
      deepAfter: 48,
      retention: 78,
    },
  },
  {
    id: 'q4-display-campaign',
    name: 'Q4 Display Campaign',
    type: 'advertising',
    period: { start: '2025-10-01', end: '2025-12-31' },
    impact: 'distorter',
    summary: '광고 기간 중 상승, 종료 후 baseline 복귀',
    metrics: {
      initialBefore: 70,
      initialAfter: 68,
      deepBefore: 30,
      deepAfter: 32,
      retention: 42,
    },
  },
];
```

---

## 9. 성공 기준

사용자가 이 메뉴를 보고 다음을 말할 수 있어야 한다:

```
✅ "이 캠페인은 의미 있었네"
✅ "이 광고는 단기 반응만 만들었네"
✅ "이 조합은 다음에도 써볼 만하네"
```

👉 **"얼마 썼는데?"가 아니라 "그래서 무슨 역할을 했는데?"**가 나오면 성공

---

## 10. 설계 원칙 요약

| 원칙 | 설명 |
|------|------|
| 성과 ❌, 원인 ⭕ | 캠페인 데이터는 성과가 아닌 원인 변수로 해석 |
| Review Flow 연결 | 모든 시각화는 단계 이동과 직접 연결 |
| 지속성 판단 | 단기 수치 증감 ❌, 종료 후 유지 여부 ⭕ |
| 역할 분류 | 미디어는 성과가 아닌 역할(Role)로 정의 |

---

*End of Campaign & Media Impact Specification*
