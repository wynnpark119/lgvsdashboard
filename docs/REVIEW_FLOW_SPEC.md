# Review Flow Specification

> **Version**: 1.0.0  
> **Last Updated**: 2026-01-23  
> **Status**: Implementation Ready  
> **Position**: Core View (대시보드의 심장부)

---

## 1. Review Flow의 정체성

### 1.1 정의

Review Flow는 VS 기술이 OEM의 디지털 접점에서 **어디까지 검토되고 있는지**를  
**'흐름'으로 해석**하는 핵심 화면이다.

```
Review Flow는
  - 초기 검토 / 심화 검토를 나누어 보여주는 곳이 ❌
  - 하나의 연속된 검토 여정에서 '현재 위치와 이동'을 보여주는 곳 ⭕
```

### 1.2 Review Flow가 답하는 4가지 질문

| # | 질문 | 해석 |
|---|------|------|
| 1 | 지금 VS 기술 검토는 어디까지 와 있는가 | 전체 위치 파악 |
| 2 | 어떤 기술이 앞으로/정체/이탈 중인가 | 방향 판단 |
| 3 | 이 흐름이 자생적인가, 광고 의존적인가 | 지속성 평가 |
| 4 | 영업·기술 조직이 개입할 지점은 어디인가 | 액션 포인트 |

### 1.3 의도적으로 배제하는 것

❌ 채널별 성과 비교  
❌ Ads KPI (CTR, CPC, Spend)  
❌ Conversion / Funnel  
❌ Lead / CRM

---

## 2. 페이지 레이아웃

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Review Flow                                  │
│                   기술 검토 흐름 대시보드 (Core View)                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ [A] Review Flow Map                                          │    │
│  │     "지금 검토가 어디쯤 와 있는가"                            │    │
│  │     X축: 검토 단계 (Initial → Deep → Reachable)              │    │
│  │     Y축: 검토 강도 (Intensity)                               │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌───────────────────────┐  ┌───────────────────────────────────┐   │
│  │ [B] Stage Signal      │  │ [C] Technology Review Cards       │   │
│  │     Summary           │  │     "지금 봐야 할 기술"            │   │
│  │     검토 시그널 강도   │  │                                   │   │
│  └───────────────────────┘  └───────────────────────────────────┘   │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ [D] Flow Trend & Stability                                   │    │
│  │     "이 흐름이 일시적인가, 방향성인가"                        │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘

위 → 아래: 추상 → 구체, 전체 → 개별
```

---

## 3. [A] Review Flow Map

### 3.1 목적
"지금 검토가 어디쯤 와 있는가"를 직관적으로 보여주는 지도

### 3.2 축 구성

| 축 | 의미 | 값 |
|----|------|-----|
| X축 | 검토 단계 | Initial → Deep → Reachable |
| Y축 | 검토 강도 | 1 (약) → 10 (강) |

### 3.3 노드(Node) 구성

각 기술/솔루션은 하나의 노드로 표현:

```typescript
interface FlowNode {
  id: string;
  name: string;
  stage: 'initial' | 'deep' | 'reachable';  // X축 위치
  intensity: number;                         // Y축 위치 (1-10)
  direction: 'advancing' | 'stable' | 'declining';  // 이동 방향
  paidDependency: 'low' | 'medium' | 'high';        // 광고 의존도 (속성)
  actionHint?: string;                              // 액션 힌트
}
```

### 3.4 노드 시각화

```
       Intensity
       (검토 강도)
          ↑
      10  │     ◉ Digital Cockpit (↑)
          │        [Paid: Low]
       8  │  ◉ Vehicle Vision (↑)
          │     [Paid: Low]
       6  │        ◉ ADAS (→)
          │           [Paid: Med]
       4  │  ◉ IVI (↑)
          │     [Paid: High]
       2  │        ◉ Telematics (↓)
          │           [Paid: Low]
          └────────────────────────────→ Stage
             Initial    Deep    Reachable
                    (검토 단계)
```

### 3.5 노드 정보 표시

| 요소 | 표시 방법 |
|------|----------|
| 기술명 | 노드 라벨 |
| 현재 위치 | X축 좌표 (Initial/Deep/Reachable) |
| 검토 강도 | Y축 좌표 + 노드 크기 |
| 이동 방향 | 화살표 (↑ 진전 / → 정체 / ↓ 약화) |
| Paid Dependency | 작은 태그 (Low/Med/High) |

### 3.6 광고 반영 방식

```
❌ 노드 색상으로 광고 표현
❌ 노드 크기로 광고 표현
⭕ Paid Dependency는 노드의 '속성'으로만 표시 (작은 태그)
```

의미:
- "이 검토 흐름이 광고로 밀린 것인지"
- "광고 없이도 유지되는 흐름인지"

---

## 4. [B] Stage Signal Summary

### 4.1 목적
"왜 이 기술이 이 위치에 있는가"를 빠르게 설명

### 4.2 시그널 정의

**Initial Review Signal** (검토 진입)
| 시그널 | 의미 |
|--------|------|
| 기술 콘텐츠 유입 | 첫 접촉 발생 |
| 체류 시간 | 2분+ = 이해 시작 |
| 영상 시청 깊이 | 50%+ = 관심 확인 |

**Deep Review Signal** (검토 심화)
| 시그널 | 의미 |
|--------|------|
| 재방문 | 30일 내 2회+ |
| 반복 콘텐츠 소비 | 3개+ 콘텐츠 |
| 웨비나 / Inquiry | 직접 접촉 의사 |

### 4.3 출력 방식

```
❌ KPI 카드 나열 (숫자 강조)
⭕ 강도(Level)로 표시: Low / Medium / High
```

```typescript
interface StageSignal {
  stage: 'initial' | 'deep';
  signals: {
    name: string;
    level: 'low' | 'medium' | 'high';
    description: string;
  }[];
  overallLevel: 'low' | 'medium' | 'high';
}
```

---

## 5. [C] Technology Review Cards

### 5.1 목적
"그래서 지금 봐야 할 기술은 무엇인가"

### 5.2 카드 구성

```typescript
interface TechnologyCard {
  id: string;
  name: string;
  stage: 'initial' | 'deep' | 'reachable';
  intensityChange: number;        // 전 기간 대비 변화
  intensityDirection: 'up' | 'stable' | 'down';
  paidDependency: 'low' | 'medium' | 'high';
  actionHint: string;             // 다음 행동 힌트
}
```

### 5.3 Action Hint 예시

| 상황 | Action Hint |
|------|-------------|
| Deep + Paid Low + 상승 | "영업 접촉 가능" |
| Deep + Paid Low + 정체 | "심화 검토 유지" |
| Initial + Paid High + 상승 | "광고 효과 검증 필요" |
| Initial + Paid Low + 하락 | "추가 콘텐츠 필요" |
| Deep + 하락 | "이탈 방지 필요" |

### 5.4 정렬 기준

```
❌ 트래픽 순
❌ 광고비 순
⭕ 검토 강도 변화 + 단계 이동 가능성 (Composite)
```

정렬 로직:
```typescript
function sortTechnologies(techs: TechnologyCard[]): TechnologyCard[] {
  return techs.sort((a, b) => {
    // 1. 단계 이동 가능성 (Deep 우선)
    const stageScore = { reachable: 3, deep: 2, initial: 1 };
    const stageDiff = stageScore[b.stage] - stageScore[a.stage];
    if (stageDiff !== 0) return stageDiff;
    
    // 2. 검토 강도 변화 (상승 우선)
    return b.intensityChange - a.intensityChange;
  });
}
```

---

## 6. [D] Flow Trend & Stability

### 6.1 목적
"이 흐름이 일시적인가, 방향성인가"

### 6.2 구성 요소

| 요소 | 기간 | 내용 |
|------|------|------|
| 전체 검토 강도 트렌드 | 30/60/90일 | 평균 Intensity 추이 |
| 단계 이동 비율 변화 | 30/60/90일 | Initial→Deep 전환율 |

### 6.3 광고 반영 방식

```
광고 집행 구간 = 배경 타임 밴드로만 표시

┌────────────────────────────────────────────────────┐
│  Intensity                                          │
│  ▲                              ┌─Campaign─┐       │
│  │     ▲───▲───▲              ▲▲▲         ▲▲     │
│  │   ▲       ▲──▲──▲──▲──▲──▲   ▲▲▲──▲▲     │
│  │ ▲                                              │
│  └──────────────────────────────────────────────→ │
│     Nov      Dec      │ CES │      Jan            │
│                       └─────┘                     │
│                       Campaign                     │
│                       Period                       │
└────────────────────────────────────────────────────┘
```

### 6.4 해석 포인트

| 패턴 | 해석 | 안정성 |
|------|------|--------|
| 캠페인 후 유지 | Organic 전환 성공 | Stable |
| 캠페인 후 급락 | 광고 의존 | Unstable |
| 캠페인 무관 상승 | 자생적 성장 | Very Stable |

---

## 7. 데이터 구조

### 7.1 Flow Map Data

```typescript
interface ReviewFlowData {
  period: string;
  nodes: FlowNode[];
  stageDistribution: {
    initial: { count: number; avgIntensity: number };
    deep: { count: number; avgIntensity: number };
    reachable: { count: number; avgIntensity: number };
  };
  overallDirection: 'advancing' | 'stable' | 'declining';
}
```

### 7.2 Demo Data

```typescript
const DEMO_FLOW_NODES: FlowNode[] = [
  {
    id: 'digital-cockpit',
    name: 'Digital Cockpit',
    stage: 'deep',
    intensity: 8.2,
    direction: 'advancing',
    paidDependency: 'low',
    actionHint: '영업 접촉 가능',
  },
  {
    id: 'vehicle-vision',
    name: 'Vehicle Vision',
    stage: 'deep',
    intensity: 7.8,
    direction: 'advancing',
    paidDependency: 'low',
    actionHint: '심화 검토 유지',
  },
  {
    id: 'adas',
    name: 'ADAS',
    stage: 'deep',
    intensity: 6.5,
    direction: 'stable',
    paidDependency: 'medium',
    actionHint: '관찰 필요',
  },
  {
    id: 'ivi',
    name: 'IVI',
    stage: 'initial',
    intensity: 5.2,
    direction: 'advancing',
    paidDependency: 'high',
    actionHint: '광고 효과 검증 필요',
  },
  {
    id: 'telematics',
    name: 'Telematics',
    stage: 'initial',
    intensity: 3.8,
    direction: 'declining',
    paidDependency: 'low',
    actionHint: '추가 콘텐츠 필요',
  },
];
```

---

## 8. 네비게이션 연결

### 8.1 Review Flow → 다음 메뉴

| 액션 | 이동 대상 | 목적 |
|------|----------|------|
| 특정 기술 클릭 | Campaign Impact | "왜 이동했는가" 확인 |
| 검토 강도 세부 클릭 | Detail | 숫자 검증 |

```
❌ 채널 페이지로 바로 이동
⭕ 항상 흐름 → 원인 → 검증 순서 유지
```

---

## 9. 성공 기준

사용자가 Review Flow를 보고 다음을 말할 수 있어야 한다:

```
✅ "이 기술은 지금 심화 단계네"
✅ "이건 광고 없으면 유지 안 되겠네"
✅ "이건 영업 붙여도 되겠다"
```

👉 이 세 문장이 나오면 Review Flow는 성공

---

*End of Review Flow Specification*
